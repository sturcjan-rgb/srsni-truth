name: debug-pages
on:
  workflow_dispatch:
jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 debug_pages.py
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
